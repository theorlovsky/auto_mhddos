{
  "name": "zx-ts",
  "version": "1.0.0",
  "description": "",
  "main": "start.ts",
  "type": "module",
  "scripts": {
    "build": "docker build -t ghcr.io/theorlovsky/auto_mhddos:experimental .",
    "dev": "docker run -it --rm $(docker build -q .)",
    "deploy": "npm run build && npm run push",
    "format": "prettier -w start.ts",
    "push": "docker push ghcr.io/theorlovsky/auto_mhddos:experimental",
    "start": "node --loader ts-node/esm --no-warnings start.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "10.7.0",
    "typescript": "4.6.2",
    "zx": "6.0.6"
  },
  "devDependencies": {
    "prettier": "2.6.0"
  }
}
